<?xml version="1.0" encoding="UTF-8"?>
<krpano version="1.19">

    
  <!-- Spot Style - Open a website -->


  <style name="IconOpenawebsite"
         url="%FIRSTXML%/spots/hotspot_information_sequence_user_defaultpoint4.png"
         visible="false"
         frame="0"
         lastframe="13"
         framewidth="40"
         frameheight="60"
         stopspotsequence="false"
         crop="0|0|40|60"
         edge="center"
         onloaded="startspotanimation(0.1);"
         onhover="onhoverIconOpenawebsite"
         onout="onoutIconOpenawebsite"

         height="80"
         width="35"
         />
  <action name="onhoverIconOpenawebsite">
    showtooltip('hotspot');
  </action>
  <action name="onoutIconOpenawebsite">
    hideTooltip();



  </action>



    
  <!-- Spot Style - Move Here 1 -->


  <style name="IconMoveHere1"
         url="%FIRSTXML%/spots/move_here_01_user_defaultpoint3.png"
         visible="false"
         edge="center"
         onhover="onhoverIconMoveHere1"
         onout="onoutIconMoveHere1"

         height="50"
         width="50"
         />
  <action name="onhoverIconMoveHere1">
    showtooltip('hotspot');
  </action>
  <action name="onoutIconMoveHere1">
    hideTooltip();



  </action>



  <!-- unsupported kind of display : PointMap -->


    
  <!-- Spot Style - Museum of History Hotspot -->


  <style name="IconMuseumofHistoryHotspot"
         url="%FIRSTXML%/spots/mmofhistory_user_defaultpoint9.png"
         visible="false"
         edge="center"
         onhover="onhoverIconMuseumofHistoryHotspot"
         onover="onoverIconMuseumofHistoryHotspot"
         onout="onoutIconMuseumofHistoryHotspot"

         height="96"
         width="175"
         />
  <action name="onhoverIconMuseumofHistoryHotspot">
    showtooltip('hotspot');
  </action>
  <action name="onoverIconMuseumofHistoryHotspot">
tween(scale, 1.2);


  </action>
  <action name="onoutIconMuseumofHistoryHotspot">
    hideTooltip();
    tween(scale, 1);


  </action>



    
  <!-- Spot Style - Enter Glover Park -->


  <style name="IconEnterGloverPark"
         url="%FIRSTXML%/spots/gogloverpark_user_defaultpoint10.png"
         visible="false"
         edge="center"
         onhover="onhoverIconEnterGloverPark"
         onout="onoutIconEnterGloverPark"

         height="100"
         width="100"
         />
  <action name="onhoverIconEnterGloverPark">
    showtooltip('hotspot');
  </action>
  <action name="onoutIconEnterGloverPark">
    hideTooltip();



  </action>



  <!-- unsupported kind of display : PointMap -->


<!-- ********** -->
<!-- Powered by Virtuary -->
<!-- id : logo -->
<!-- ********** -->





<!-- ********** -->
<!-- Visit Marietta! -->
<!-- id : logo1 -->
<!-- ********** -->


  <hotspot ath="0" atv="90"
         zorder="0"
         name="logo1"
         keep="true"
         visible="false"
         capture="false"
         url="%FIRSTXML%/graphics/logo1/marietta_square.png" 
         handcursor="false"

         height="200"
         width="200"

         />

  <events name="logo1events" 
    onTourStart="showlogo1();"
    keep="true" />

  <action name="hidelogo1">
    set(hotspot[logo1].visible, false);
  </action>
  <action name="showlogo1">
      set(hotspot[logo1].visible, true);
  </action>



  <events name="logo1showevents" onremovepano="hidelogo1" onPanoStart="showlogo1" keep="true"/>




<!-- ********** -->
<!-- Marietta Welcomes You! -->
<!-- id : logo2 -->
<!-- ********** -->





  <!-- FLOORPLAN -->
  <action name="hideFloorplan">set(tour_displayfloorplan, false);events.dispatch(onhidetourfloorplan);</action>
  <action name="showFloorplan">set(tour_displayfloorplan, true); events.dispatch(onshowtourfloorplan);</action>


  <!-- Fullscreen Management -->
  <action name="exitFullScreen" devices="fullscreensupport">set(tour_fullscreen,false);js(setFullscreen(false));</action>
  <action name="enterFullScreen" devices="fullscreensupport">set(tour_fullscreen,true);js(setFullscreen(true));</action>
  <action name="switchFullScreen">if(tour_fullscreen,exitFullScreen();,enterFullScreen(););</action>

  
  <events name="krpanoExitFullscreenEvent"
    onexitfullscreen="if(tour_fullscreen,exitFullScreenChangeEvent(););"
    keep="true" />

  <action name="exitFullScreenChangeEvent" devices="fullscreensupport">set(tour_fullscreen,false);events.dispatch(onexitfullscreen);</action>
  <action name="enterFullScreenChangeEvent" devices="fullscreensupport">set(tour_fullscreen,true);events.dispatch(onenterfullscreen);</action>
  <action name="exitFullScreenFallback">set(fullscreen,false);</action>
  <action name="enterFullScreenFallback">set(fullscreen,true);</action>
  
  <action name="resizeFullScreenEvent">delayedcall(0,updatescreen(););</action>

<action name="viewControlMoveUp">
	if(%1,
		if(%2,
			set(isfullvertical, false);
			ifnot(scene[get(xml.scene)].planar,
				sub(viewvfov, panoview.vmax, panoview.vmin);
				if (viewvfov == 180,
					set(isfullvertical, true);
				);
			);
			if(isfullvertical == false AND calc(view.vlookat-%2) LT view.vlookatmin,
				moveto(get(view.hlookat), get(view.vlookatmin));
			,
				moveto(get(view.hlookat), calc(view.vlookat-%2));
			);
		,
			set(vlookat_moveforce, -1);
			delayedcall(0.1, viewControlMoveVertStop());
		);
	,
		set(vlookat_moveforce, -1);
	);
</action>
<action name="viewControlMoveDown">
	if(%1,
		if(%2,
			set(isfullvertical, false);
			ifnot(scene[get(xml.scene)].planar,
				sub(viewvfov, panoview.vmax, panoview.vmin);
				if (viewvfov == 180,
					set(isfullvertical, true);
				);
			);
			if(sceneisfull360 == false AND calc(view.vlookat+%2) GT view.vlookatmax,
				moveto(get(view.hlookat), get(view.vlookatmax));
			,
				moveto(get(view.hlookat), calc(view.vlookat+%2));
			);
		,
			set(vlookat_moveforce, +1);
			delayedcall(0.1, viewControlMoveVertStop());
		);
	,
		set(vlookat_moveforce, +1);
	);
</action>
<action name="viewControlMoveLeft">
	if(%1,
		if(%2,
			set(isfullhorizontal, false);
			ifnot(scene[get(xml.scene)].planar,
				sub(viewhfov, panoview.hmax, panoview.hmin);
				if (viewhfov == 360,
					set(isfullhorizontal, true);
				);
			);
			if(isfullhorizontal == false AND calc(view.hlookat-%2) LT view.hlookatmin,
				moveto(get(view.hlookatmin), get(view.vlookat));
			,
				moveto(calc(view.hlookat-%2), get(view.vlookat));
			);
		,
			set(hlookat_moveforce, -1);
			delayedcall(0.1, viewControlMoveHoriStop());
		);
	,
		set(hlookat_moveforce, -1);
	);
</action>
<action name="viewControlMoveRight">
	if(%1,
		if(%2,
			set(isfullhorizontal, false);
			ifnot(scene[get(xml.scene)].planar,
				sub(viewhfov, panoview.hmax, panoview.hmin);
				if (viewhfov == 360,
					set(isfullhorizontal, true);
				);
			);
			if(isfullhorizontal == false AND calc(view.hlookat+%2) GT view.hlookatmax,
				moveto(get(view.hlookatmax), get(view.vlookat));
			,
				moveto(calc(view.hlookat+%2), get(view.vlookat));
			);
		,
			set(hlookat_moveforce, +1);
			delayedcall(0.1, viewControlMoveHoriStop());
			
		);
	,
		set(hlookat_moveforce, +1);
	);
</action>
<action name="viewControlMoveHoriStop">
	set(hlookat_moveforce, 0);
</action>
<action name="viewControlMoveVertStop">
	set(vlookat_moveforce, 0);
</action>

<action name="viewControlZoomIn">
	if(%1,
		if(%2,
			if(%2 LT view.fovmin,
				zoomto(get(view.fovmin));
			,
				if(%2 LT view.fov,
					zoomto(%2);
				);
			);
		,
			set(fov_moveforce, -1);
			delayedcall(0.1, viewControlZoomStop());
		);
	,
		set(fov_moveforce, -1);
	);
</action>
<action name="viewControlZoomOut">
	if(%1,
		if(%2,
			if(%2 GT view.fovmax,
				zoomto(get(view.fovmax));
			,
				if(%2 GT view.fov,
					zoomto(%2);
				);
			);
		,
			set(fov_moveforce, +1);
			delayedcall(0.1, viewControlZoomStop());
		);
	,
		set(fov_moveforce, +1);
	);
</action>
<action name="viewControlZoomStop">
	set(fov_moveforce, 0);
</action>

  <!-- Hide / Show Thumbnails -->
  <action name="hideTourThumbnails">set(tour_displaythumbnails, false);events.dispatch(hidetourthumbnails);</action>
  <action name="showTourThumbnails">set(tour_displaythumbnails, true); events.dispatch(showtourthumbnails);</action>


  <!-- Hide / Show Controls -->
  <action name="hideTourControls">set(tour_displaycontrols, false);events.dispatch(hidetourcontrols);</action>
  <action name="showTourControls">set(tour_displaycontrols, true); events.dispatch(showtourcontrols);</action>


</krpano>